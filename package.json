{
  "name": "back",
  "version": "1.0.0",
  "description": "back jump app",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "NODE_ENV=test && echo 'Running app.js on NODE_ENV='${NODE_ENV} && npm run start",
    "prod": "NODE_ENV=prod && echo 'Running app.js on NODE_ENV='${NODE_ENV} && npm run start",
    "dev": "NODE_ENV=dev && echo 'Running app.js on NODE_ENV='${NODE_ENV} && nodemon app.js",
    "setup": "NODE_ENV=dev && echo 'Running setup.js on NODE_ENV='${NODE_ENV} && node setup.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "config": "^2.0.1",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "errorhandler": "^1.5.0",
    "express": "^4.16.4",
    "express-jwt": "^5.3.1",
    "install": "^0.12.2",
    "jsonwebtoken": "^8.3.0",
    "morgan": "^1.9.1",
    "mysql2": "^1.6.3",
    "npm": "^6.4.1",
    "passport": "^0.4.0",
    "passport-jwt": "^4.0.0",
    "passport-local": "^1.0.0",
    "sequelize": "^4.41.1"
  },
  "devDependencies": {
    "eslint": "^5.8.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.14.0",
    "nodemon": "^1.18.6"
  }
}
